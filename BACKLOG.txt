
INSERIR EM MÉTRICAS AQUELAS QUE NÃO ESTÃO LISTADAS NOS RESULTADOS, complementar
SE DER TEMPO, FAZER OS MODELOS FICARAM EM ORDEM DO MAPEAMENTO NO TRADEOFF
- EU ESTOU FAZENDO AS DIFERENÇAS NO TRADEOFF DO BASELINE PARA A METRICA MELHOR PARA O GRUPO FEMININO
NO TENTANDO O MELHOR PODERIA SER 0, QUE, EM AVERAGE ODDS DIFFERENCE ESTÁ SENDO CONQUISTADO.
- colocar as métricas utilizadas na planilha de métricas do inicio de experimentos
-------------------
\begin{comment}
Todos as classes aceitam sample_weight, PARA JUSTIFICAR REW


LogisticRegression -  fit(X, y, sample_weight=None)
	sample_weightarray-like of shape (n_samples,) default=None
	Array of weights that are assigned to individual samples. If not provided, then each sample is given unit weight.

MLPClassifier - fit(X, y, sample_weight=None)
	sample_weightarray-like of shape (n_samples,), default=None
	Sample weights.

LinearSVC - fit(X, y, sample_weight=None)
	sample_weightarray-like of shape (n_samples,), default=None
	Array of weights that are assigned to individual samples. If not provided, then each sample is given unit weight

DecisionTreeClassifier - fit(X, y, sample_weight=None, check_input=True)
	sample_weight
	array-like of shape (n_samples,), default=None
	Sample weights. If None, then samples are equally weighted. Splits that would create child nodes with net zero or 	negative weight are ignored while searching for a split in each node. Splits are also ignored if they would result 	in any single class carrying a negative weight in either child node.

RandomForestClassifier - fit(X, y, sample_weight=None)

	sample_weight
	array-like of shape (n_samples,), default=None
	Sample weights. If None, then samples are equally weighted. Splits that would create child nodes with net zero or 	negative weight are ignored while searching for a split in each node. In the case of classification, splits are 	also ignored if they would result in any single class carrying a negative weight in either child node.

XGBClassifier
	fit(X, y, *, sample_weight=None, base_margin=None, eval_set=None, verbose=True, xgb_model=None, 	sample_weight_eval_set=None, base_margin_eval_set=None, feature_weights=None
        sample_weight (Any | None) – instance weights

BernoulliNB - fit(X, y, sample_weight=None)
	sample_weight
	array-like of shape (n_samples,), default=None
	Weights applied to individual samples (1. for unweighted).

\end{comment}