dataset:
  name: "adult"
  path: "datasets/adult_sklearn/adult_sklearn.csv"
  target: "income"
  sensitive: "sex"
  privileged: ["Male"]    
  unprivileged: ["Female"]
  favorable: ">50K"   
  unfavorable: "<=50K" 

split:
  test_size: 0.2
  random_state: 50

model:
  name: random_florest
  params: 
     random_state: 50


  #name: "logistic_regression" 
  #params:
  #  max_iter: 2000
  #  random_state: 50

  #name: "neural_network"
  #params:
  #  hidden_layer_sizes: [128, 64]
  #  activation: "relu"
  #  solver: "adam"
  #  alpha: 0.0001
  #  batch_size: 32
  #  learning_rate_init: 0.001
  #  max_iter: 300
  #  early_stopping: true
  #  validation_fraction: 0.1
  #  n_iter_no_change: 10
  #  random_state: 50

mitigation:
  pre:
    name: "reweighing" #"none" 
    params: {}
  in:
    name: "none"
    params: {}
  post:
    name: "none"
    params: {}

metrics:
  performance:
    - accuracy
    - precision
    - recall
    - f1
    - roc_auc
    - pr_auc
  fairness:
    - statistical_parity_diff
    - equalized_odds_diff
    - disparate_impact
